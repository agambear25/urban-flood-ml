# Large rasters / per-city data — regenerated by the pipeline
data/
*.tif
*.tiff

# MLflow local store (run `mlflow ui` after `floodml train`)
mlruns/
mlflow.db

# osmnx / API response cache
cache/
**/cache/

# Python
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/
build/
dist/

# OS / EE
.DS_Store
*.credentials
